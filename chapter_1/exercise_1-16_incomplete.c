// This exercise is to modify a program which doesn't actually compile because the book is old
// The issue is caused by something I haven't learned yet, so I'll come back to this one later

// Update: It is actually because stdio.h defines getline now, and K&R were trying to redefine it using the same name, causing an error between the two function definitions
// I don't feel like doing this one, so I just won't